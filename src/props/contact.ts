export interface ContactValues {
    // The Email For The Contact Form
    email: string;
  
    // The Subject For The Contact Form
    subject: string;
  
    // The Message For The Contact Form
    message: string
  }