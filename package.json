{
  "name": "drivn-dev-types",
  "private": false,
  "version": "1.0.130",
  "description": "The types for drivn",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx tsc",
    "github": "git add . && git commit -m \"updated types\" && git push origin develop",
    "prepublishOnly": "npx tsc && npm run github && npm version patch",
    "buildAndPublish": "npx tsc && git add . && git commit -m \"updated types\" && git push origin develop && npm version patch && npm publish"
  },
  "type": "module",
  "types": "dist/index.d.ts",
  "files": [
    "dist/*/**",
    "dist/index.d.ts"
  ],
  "repository": {
    "type": "git"
  },
  "author": "drivndev",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.21",
    "grunt-bump": "^0.8.0",
    "grunt-contrib-jshint": "^3.2.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@types/react": "^18.2.55",
    "express": "^4.18.2"
  }
}
